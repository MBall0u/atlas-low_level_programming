#include "main.h"
#include <stdio.h>
/**
*/
char *_strstr(char *haystack, char *needle)
{
}

/* for the string haystack it needs to iterate character by character
checking each one against needle and once it matches the first character
of needle with a character in haystack it checks the next characters
against needle till a null byte is met in needle meaning the entire string
has been found
*/

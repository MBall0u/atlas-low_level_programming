#include <unistd.h>
#include "main.h"
/**
* swap_int - Don't swap horses in crossing a stream
* Description - Write a function that swaps the values of two integers
* @a: switching with variable b
* @b: switching with variable a
* Return: Always 0 (success)
*/
void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
